Copy *.jira-env.example* to *.jira-env* and populate it with valid data. Run with `docker compose up --build -d`. Access the app via *localhost:5000*. Data is preserved when tab closes

All credit goes to Benjamin Bajic